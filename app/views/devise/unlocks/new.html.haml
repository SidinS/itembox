- title t('.resend_unlock_instructions', default: 'Resend unlock instructions')

= stylesheet_link_tag 'bootstrap_and_overrides', media: 'all'
= stylesheet_link_tag 'sessions', media: 'all'
= javascript_include_tag 'application'

- if devise_error_messages!.present? || flash[:alert].present?
  .row
    .span11.offset1
      .alert.alert-error
        %button.close{"data-dismiss" => "alert", :type => "button"} ×
        = devise_error_messages!
        = flash[:alert]
.container
  = form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { class: 'form-signin', method: :post }) do |f|
    %h2.form-signin-heading= t('.resend_unlock_instructions', default: 'Resend unlock instructions')
    = f.email_field :email, class: 'input-block-level', placeholder: t('activerecord.attributes.user.email')
    = f.submit t('.resend_unlock_instructions', default: 'Resend unlock instructions'),  class: 'btn btn-large btn-primary'
    %p
    %p.text-right= render 'devise/shared/links'
    %p
